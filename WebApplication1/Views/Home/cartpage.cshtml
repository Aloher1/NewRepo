<!DOCTYPE html>

<html>
<head>
    <link href="~/StyleSheet1.css" rel="stylesheet" type="text/css" />
    <title>Корзина</title>
</head>
<body>
    <div class="main">
        <div class=" header">
            <div><h1>Корзина</h1></div>
        </div>
        <menu class="menu">
            <li><a href="#">жми сюда</a></li>
            <li>
                <a href="#">или сюда</a>
                <div>
                    <a href="#">qwerty</a>
                    <a href="#">йцукен</a>
                </div>
            </li>
        </menu>
        <form method="post">
            <div class="cartcontent">
                <div class="cartproducts">
                @{  
                    for(int i = 0;i < ViewBag.cartlist.Count;i++){
                        string path = "Pictures/" + ViewBag.cartlist[i].name + ".jpg";
                        <div class="cart">
                            <input type="checkbox" name="checkboxlist" value="@ViewBag.cartlist[i].id" />
                            <img src=~/@path />
                            <div>
                                <h2>@ViewBag.cartlist[i].name</h2>
                                <p>@ViewBag.cartlist[i].price р.</p>
                                <p>Количество: @ViewBag.cartlist[i].amount</p>
                            </div>
                        </div>
                    }   
                }
                </div>
                <div class="purchase">
                    <h3>Итог:</h3>
                    <p><input type="submit" name="button" value="Купить" /></p>
                </div>
            </form>

        </div>   
    </div>
</body>
</html>