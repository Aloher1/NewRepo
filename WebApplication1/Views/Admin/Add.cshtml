<!DOCTYPE html>
<head>
    <style>
        body {
            background: #82b1cc;
            text-align: center;
        }

        .main {
            margin: 0 auto 0 auto;
            padding: 3px;
            background: #d1d1d1;
            text-align: center;
            border: 2px solid black;
            display:flex;
            flex-direction: row;
            justify-content: center;
        }
        .OK{
            position: absolute;
            z-index:  100;
            top:30%;
            left:33%;
            margin: 0 auto 0 auto;
            padding: 3px;
            background: #82b1cc;
            text-align: center;
            border: 2px solid black;
            width:35%;
            height:20%;
        }
        
    </style>
</head>
<body>
    <div class="main">
        <form method="get">
            <div>
                <p><a>Название товара</a></p>
                <p><input type="text" size="20" name=name></p>
                <p><a>Цена</a></p>
                <p><input type="text" size="20" name=price></p>
                <p><select name="category">
                    <option>Платы</option>
                    <option>Корпуса</option>
                </select></p>
                <p><input type="file" size="10" name="image" value="выберите файл" /></p>
            </div>
            <div>
                <p><input type="text" size="20" name="description" value="Описание"</p>
            </div>
            <p><input type="submit" value="Добавить" size="10" name=button></p>
        </form>
    </div>
    @{
        if (ViewBag.save == true)
        {
            <div class="OK">
                <h1>Успешно Сохранено</h1>
            </div>
        }
        else if (ViewBag.save == false)
        {
            <div class="OK">
                <h1>ошибка ничего не сохранилось</h1>
            </div>
        }
    }
</body>